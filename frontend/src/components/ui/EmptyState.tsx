/**
 * Unified EmptyState (Phase D-02).
 * Re-exports the atelier product empty state (no emoji chrome by default).
 * Pages should import from `components/EmptyState` or `components/ui`.
 */
export { EmptyState } from '../EmptyState'
