# VisionSetil Frontend Environment Variables
# Copy to .env.local and customize

# Backend API URL (use /api in dev for proxy, full URL in prod)
VITE_API_URL=/api

# API Key (only if backend has API_KEYS enabled)
VITE_API_KEY=